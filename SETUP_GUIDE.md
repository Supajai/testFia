# คู่มือการใช้งานระบบสร้างบัตรประจำตัวจากไฟล์ Excel (Excel ID Card Badge Manager)

ระบบได้รับการเปลี่ยนผ่านเข้าสู่ **ระบบไฟล์ Excel 100% (Local Excel Mode)** ทำงานแบบ **ออฟไลน์ 100% (ไม่ต้องต่ออินเทอร์เน็ต)** ข้อมูลทั้งหมดถูกจัดเก็บและประมวลผลอย่างปลอดภัยในเครื่องคอมพิวเตอร์ของคุณ

---

## 🌟 คุณสมบัติเด่นของระบบ Excel Mode:

1. **เปิดไฟล์ Excel ได้โดยตรง (.xlsx, .xls, .csv):**
   - รองรับไฟล์ตารางข้อมูลของหน่วยงานทุกรูปแบบ
   - รองรับไฟล์ Excel ที่มีหลายแผ่นงาน (Multi-Sheet Tabs) พร้อมตัวเลือกสลับแผ่นงานได้ทันที
2. **แก้ไขและบันทึกทับลงไฟล์ Excel ทันที:**
   - แก้ไขข้อความหรือเปลี่ยนรูปภาพในโปรแกรม แล้วกดปุ่ม **"บันทึกไฟล์ Excel"** ข้อมูลจะถูกเขียนอัปเดตลงในไฟล์ `.xlsx` นั้นโดยตรง
3. **ระบบจัดการรูปภาพถ่ายประจำตัว:**
   - กดปุ่ม **"เลือกรูปภาพจากเครื่องคอมพิวเตอร์ (.jpg, .png)"** เพื่อเลือกรูปถ่ายประจำตัวของแต่ละคน
   - ระบบมี **Smart Auto-Crop** ช่วยจัดกึ่งกลางและปรับสัดส่วนใบหน้าอัตโนมัติ ทำให้ภาพคมชัด สวยงาม ไม่มียืด 100%
4. **ฟอนต์มาตรฐานราชการไทยสารบรรณแท้ 100% (TH Sarabun New):**
   - ฝังไฟล์ฟอนต์ **TH Sarabun New** (Regular, Bold, Italic, Bold-Italic) ตัวจริงในโปรแกรม ทำงานแบบออฟไลน์ได้ทันที ตัวอักษรมีหัวโค้งมนสวยงามตามระเบียบงานสารบรรณราชการไทย 100%
5. **โหมดปรับแต่งตำแหน่งอิสระด้วยเมาส์ (Visual Drag & Resize):**
   - กดปุ่ม **"🎨 ปรับตำแหน่งอิสระ (Drag & Resize)"** เพื่อใช้เมาส์คลิกลากย้ายวัตถุทุกชิ้นบนบัตรได้อย่างอิสระ ไม่บังหัวป้าย 100%
   - **ปรับขนาดรูปถ่ายและโลโก้ได้ทั้งความกว้างและความสูง (Width & Height)** โดยใช้แถบสไลเดอร์ หรือคลิกลากจุดมุมขวาล่างของรูปภาพ/โลโก้ได้ทันที
   - จัดแต่งฟอนต์ครบวงจร: สลับตัวหนา (Bold), ตัวเอียง (Italic), เปลี่ยนสีตัวอักษร, และจัดชิดซ้าย/กึ่งกลาง/ชิดขวา
6. **ระบบบันทึกและจัดการเทมเพลต (Template Manager):**
   - บันทึกการจัดวางตำแหน่ง ขนาด และรูปแบบตัวอักษรที่ชอบเก็บเป็นเทมเพลตส่วนตัวได้ไม่จำกัด
   - สลับใช้งานเทมเพลตที่บันทึกไว้ได้ทันทีในคลิกเดียว
7. **กำหนดขนาดและส่งออกบัตรความละเอียดสูง:**
   - ขนาดมาตรฐานตามแบบ: **$5.5 \times 8.5\text{ ซม.}$ ($55 \times 85\text{ มม.}$)** แบบขอบเหลี่ยม (Sharp Corners) หรือกำหนดขนาดเอง (Custom mm)
   - ส่งออกเป็นไฟล์ **PDF** หรือ **PNG 300+ DPI** สำหรับนำไปพิมพ์บนเครื่องพิมพ์บัตร PVC ได้ทันที

---

## 🚀 วิธีเปิดใช้งานโปรแกรม:

1. เปิด Terminal / Command Prompt ในโฟลเดอร์ `c:\test_1`
2. รันคำสั่งเปิดโปรแกรม:
   ```cmd
   cmd /c npm start
   ```
3. กดปุ่มสีน้ำเงิน **"เลือกไฟล์ Excel (.xlsx)"** ที่มุมบนขวา
4. เลือกไฟล์ Excel ข้อมูลของคุณ (หรือทดลองเลือกไฟล์ตัวอย่าง `sample_employees.xlsx` ในโฟลเดอร์นี้)
5. รายชื่อและข้อมูลทั้งหมดจะถูกโหลดขึ้นมาแสดงผลบนโปรแกรมทันที!

---

## 📋 โครงสร้างหัวตาราง Excel ที่แนะนำ:

คุณสามารถตั้งชื่อหัวคอลัมน์ใน Excel เป็นภาษาไทยได้ตามต้องการ เช่น:
- `ชื่อ-นามสกุล` (หรือ `ชื่อผู้สมัคร`, `ชื่อ`)
- `ตำแหน่ง` (หรือ `สังกัด`, `แผนก`)
- `วันเกิด`
- `หมู่เลือด`
- `เบอร์โทร`
- `อีเมล`
- `รูปถ่าย` (หรือ `รูปภาพ`)
- *คอลัมน์อื่นๆ เพิ่มเติมได้ตามต้องการ ระบบจะแสดงผลอัตโนมัติ*
